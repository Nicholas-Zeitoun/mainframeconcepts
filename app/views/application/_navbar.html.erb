<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <!-- Logo -->
    <%= link_to root_path, class: "d-flex align-items-center navbar-brand" do %>
      <%= image_tag("jenay-miller.png", class: "avatar") %>
      <h3 class="m-0">Mainframe Concepts</h3>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">

        <!-- Core Concepts -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Core
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% @concepts = Concept.all %>
            <% @concepts.each do |concept| %>
              <% if concept.category == "Core" %>
                <%= link_to concept.title, concept_path(concept), class: "dropdown-item"  %>
              <% end %>
            <% end %>

            <!-- <div class="dropdown-divider"></div> -->
            <%#= link_to 'Core Concepts', concepts_path, class: "dropdown-item"  %>
          </div>
        </li>

        <!-- Core Concepts -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Languages
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% @concepts = Concept.all %>
            <% @concepts.each do |concept| %>
              <% if concept.category == "Language" %>
                <%= link_to concept.title, concept_path(concept), class: "dropdown-item"  %>
              <% end %>
            <% end %>

            <!-- <div class="dropdown-divider"></div> -->
            <%#= link_to 'Languages', concepts_path, class: "dropdown-item"  %>
          </div>
        </li>

        <!-- Resources -->
        <li class="nav-item">
          <%= link_to 'Resources', resources_path, class: "nav-link"  %>
        </li>

        <!-- OCADs -->
        <li class="nav-item">
          <%= link_to 'OCADs', acronyms_path, class: "nav-link"  %>
        </li>

      <!-- OCAD Search Bar -->
      </ul>
      <%= form_tag acronyms_path, method: :get, class: "form-inline my-2 my-lg-0" do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control mr-sm-2",
          placeholder: "O.C.A.D Search"%>
        <%= submit_tag 'Search', class: "btn btn-outline-success my-2 my-sm-0" %>
      <% end %>
    </div>
  </div>
</nav>
